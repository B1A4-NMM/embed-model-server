# Flask 웹 프레임워크
Flask==3.1.1

# Sentence Transformers (핵심 라이브러리)
sentence-transformers==4.1.0

# NumPy (수치 계산)
numpy==2.3.1

# PyTorch (딥러닝 프레임워크)
torch==2.7.1

# Transformers (Hugging Face)
transformers==4.52.4

# Tokenizers (텍스트 토큰화)
tokenizers==0.21.2

# Hugging Face Hub (모델 다운로드)
huggingface-hub==0.33.1

# Requests (HTTP 요청)
requests==2.32.4

# Werkzeug (WSGI 유틸리티)
Werkzeug==3.1.3

# Jinja2 (템플릿 엔진)
Jinja2==3.1.6

# MarkupSafe (보안)
MarkupSafe==3.0.2

# Click (명령행 인터페이스)
click==8.2.1

# ItsDangerous (보안)
itsdangerous==2.2.0

# Blinker (시그널링)
blinker==1.9.0

# Certifi (SSL 인증서)
certifi==2025.6.15

# Charset Normalizer (인코딩)
charset-normalizer==3.4.2

# Idna (도메인 이름)
idna==3.10

# Urllib3 (HTTP 클라이언트)
urllib3==2.5.0

# Tqdm (진행률 표시)
tqdm==4.67.1

# Regex (정규표현식)
regex==2024.11.6

# Safetensors (모델 저장)
safetensors==0.5.3

# Scikit-learn (머신러닝 유틸리티)
scikit-learn==1.7.0

# Scipy (과학 계산)
scipy==1.16.0

# Joblib (병렬 처리)
joblib==1.5.1

# Threadpoolctl (스레드 풀)
threadpoolctl==3.6.0

# Pillow (이미지 처리)
Pillow==11.2.1

# NetworkX (그래프 라이브러리)
networkx==3.5

# Sympy (수학)
sympy==1.14.0

# Mpmath (수학)
mpmath==1.3.0

# Fsspec (파일 시스템)
fsspec==2025.5.1

# Filelock (파일 잠금)
filelock==3.18.0

# Packaging (패키징)
packaging==25.0

# PyYAML (YAML 파서)
PyYAML==6.0.2

# Colorama (터미널 색상)
colorama==0.4.6

# Setuptools (패키지 관리)
setuptools==80.9.0

# Pip (패키지 설치)
pip==24.0

# Pkg Resources (패키지 리소스)
pkg_resources==1.0.0 